<Page
    x:Class="Shasta.Views.ItemDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shasta.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16,16,16,24">
        <StackPanel>
            <Image x:Name="CoverImage"
                   Width="160" Height="160"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,16" />

            <TextBlock x:Name="TitleText" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,4" />
            <TextBlock x:Name="AuthorText" Style="{StaticResource BodyTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Margin="0,0,0,4" />
            <TextBlock x:Name="SeriesText" Style="{StaticResource CaptionTextBlockStyle}" Opacity="0.7" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,0,0,16" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Button x:Name="PlayButton" Content="Play" Click="PlayButton_Click" Margin="0,0,8,0" />
                <Button x:Name="DownloadButton" Content="Download" Click="DownloadButton_Click" />
            </StackPanel>
            <TextBlock x:Name="DownloadStatusText"
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Opacity="0.7"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Margin="0,0,0,16" />

            <Border Style="{StaticResource SectionDividerStyle}" />

            <TextBlock x:Name="DescriptionText" Style="{StaticResource BodyTextBlockStyle}" TextWrapping="Wrap" Margin="0,0,0,16" />

            <TextBlock x:Name="ChaptersTitle"
                       Text="Chapters"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       Visibility="Collapsed"
                       Margin="0,0,0,12" />
            <StackPanel x:Name="ChaptersPanel" Visibility="Collapsed" />

            <TextBlock x:Name="StatusText"
                       Style="{StaticResource BodyTextBlockStyle}"
                       Opacity="0.7"
                       TextWrapping="Wrap" />
        </StackPanel>
    </ScrollViewer>
</Page>
