<Page
    x:Class="Shasta.Views.CollectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Shasta.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Collections"
                   Grid.Row="0"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="16,16,16,8" />

        <TextBlock x:Name="StatusText"
                   Grid.Row="1"
                   Style="{StaticResource BodyTextBlockStyle}"
                   Opacity="0.7"
                   TextWrapping="Wrap"
                   Margin="16,0,16,16"
                   VerticalAlignment="Top" />

        <!-- Collections have no cover of their own — the first book's
             cover stands in, same as SeriesPage. -->
        <GridView x:Name="CollectionsGridView"
                  Grid.Row="1"
                  Padding="8,0,8,16"
                  Visibility="Collapsed"
                  IsItemClickEnabled="True"
                  ItemClick="CollectionsGridView_ItemClick"
                  ContainerContentChanging="CollectionsGridView_ContainerContentChanging">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="150" Margin="8">
                        <Border x:Name="CoverImage"
                                Width="150" Height="150"
                                Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" />
                        <TextBlock Text="{Binding Name}"
                                   Style="{StaticResource BodyTextBlockStyle}"
                                   TextTrimming="CharacterEllipsis"
                                   Margin="0,8,0,0" />
                        <TextBlock Text="{Binding BookCountText}"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Opacity="0.7"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
